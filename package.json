{
  "name": "express-postgres-sequelize",
  "version": "1.0.0",
  "description": "A simple payment application using nodejs, express, postgres, sequelize, stripe",
  "main": "app.js",
  "scripts": {
    "make-model": "npx sequelize-cli model:generate --name Channel --attributes firstName:string,lastName:string,email:string",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "npx sequelize-cli db:migrate",
    "down": "npx sequelize-cli db:migrate:undo"
  },
  "author": "Adegbokan",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.7.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.14.1"
  },
  "devDependencies": {
    "dotenv": "^14.3.2",
    "sequelize-cli": "^6.4.1"
  }
}
